"""Neighborhood Insights package.

Issue #124 — given the active search query + the result set, surface a
panel summarizing the area: median rent, listings-by-source breakdown,
nearby SkyTrain stations, school catchments overlapping the area.

All inputs come from data already in the repo (Vancouver Open Data,
VSB GeoJSON, TransLink slim stops). No external APIs.
"""
