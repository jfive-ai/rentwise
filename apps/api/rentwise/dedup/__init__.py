"""Phase 4 PR-C: cross-source deduplication.

Given an enriched listing, find existing listings that describe the
same property and assign them a shared ``canonical_id``. See
:mod:`rentwise.dedup.service` for the entry point.
"""
